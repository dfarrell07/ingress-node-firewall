version: "2"
linters:
  default: standard
  exclusions:
    rules:
      - linters: [staticcheck]
        text: "ST1001"
      - linters: [staticcheck]
        text: "QF1001"
      - linters: [staticcheck]
        text: "QF1003"
      - linters: [staticcheck]
        text: "QF1008"
      - linters: [staticcheck]
        text: "SA1019"
      - linters: [staticcheck]
        text: "SA4010"
      - linters: [staticcheck]
        text: "S1039"
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - (io.Closer).Close
        - (*os.File).Close
        - (net.Conn).Close
        - (net.Listener).Close
        - (io.WriteCloser).Close
        - (*io.PipeWriter).Close
        - (*crypto/tls.Conn).Close
