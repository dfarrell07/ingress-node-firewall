version: "2"
linters:
  default: standard
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - (*os.File).Close
        - (io.Closer).Close
        - (*io.PipeWriter).Close
        - (net.Conn).Close
        - (*crypto/tls.Conn).Close
        - (net.Listener).Close
    staticcheck:
      checks:
        - "all"
        - "-QF*"
        - "-ST1001"
        - "-SA1019"
        - "-ST1000"
        - "-ST1003"
        - "-ST1020"
        - "-ST1021"
